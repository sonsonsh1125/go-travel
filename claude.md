# Go-Travel 프로젝트 명세서

## 프로젝트 개요

YouTube에서 특정 여행지 관련 영상을 검색하고, 각 영상의 자막에서 언급된 장소들을 AI로 자동 추출하여 웹 화면에 표시하는 시스템

## 핵심 기능

### 1. 웹 인터페이스
- Next.js 15 기반 반응형 웹 애플리케이션
- 사용자가 여행지 입력 (예: "교토", "후쿠오카", "오사카")
- 실시간 진행 상황 표시
- 검색 결과를 카드 형식으로 표시

### 2. YouTube 영상 검색
- 입력된 여행지를 기반으로 YouTube 검색
  - 검색 쿼리: "{여행지} 여행", "{여행지} 브이로그", "{여행지} 관광"
- **필터링 조건**:
  - 조회수 상위 20개 영상
  - 타이틀에 검색어가 포함된 영상만 선택
- 수집 정보: 영상 ID, 제목, 채널명, 조회수, URL

### 3. 자막(Transcript) 수집
- youtube-transcript 라이브러리 사용
- 각 영상의 자막 추출 (한국어 우선)
- 자막이 없는 영상은 자동 스킵

### 4. AI 장소 추출 (핵심 기능)
- **OpenAI GPT-4o-mini** 사용
- 자막에서 **구체적인 상호명이 있는 장소**만 추출
- **엄격한 필터링 규칙**:
  - ✅ 구체적인 상호명 (예: "이치란 라멘", "니시키 시장")
  - ❌ 일반 지역명 제외 (예: "교토", "오사카")
  - ❌ 호텔/숙박시설 제외
  - ❌ 검색 지역 외부 장소 제외
  - ❌ 애매한 표현 제외 (예: "그 카페", "유명한 곳")

### 5. 결과 표시 + 구글 지도 연동
- 추출된 장소를 웹 화면에 카드 형식으로 표시
- 각 장소마다 다음 정보 제공:
  - 장소명
  - 카테고리 (맛집/관광지/사찰/카페/쇼핑/액티비티/기타)
  - 설명 (자막에서 추출)
  - 언급 횟수
  - 언급된 영상 제목
  - **📍 지도 버튼** → 클릭 시 구글 지도에서 검색

## 기술 스택

### Frontend
- React 18
- Next.js 15 (App Router)
- Tailwind CSS

### Backend
- Next.js API Routes
- TypeScript

### APIs
- YouTube Data API v3 - 영상 검색
- youtube-transcript - 자막 수집
- OpenAI GPT-4o-mini - AI 장소 추출
- Google Maps Search API - 장소 위치 검색

### Deployment
- Vercel

## 환경 변수

```env
# YouTube Data API v3
YOUTUBE_API_KEY=your_youtube_api_key

# OpenAI API
OPENAI_API_KEY=your_openai_api_key

# AI Model (선택사항)
AI_MODEL=gpt-4o-mini
```

## 프로젝트 구조

```
go-travel/web/
├── app/
│   ├── api/search/route.ts    # 메인 API (YouTube + AI 추출)
│   ├── page.tsx                # 메인 페이지
│   └── layout.tsx              # 루트 레이아웃
├── components/
│   ├── SearchForm.tsx          # 검색 폼
│   └── Results.tsx             # 결과 표시 (지도 링크)
├── lib/
│   ├── services/
│   │   ├── youtube.ts          # YouTube 검색
│   │   ├── transcript.ts       # 자막 수집
│   │   └── placeExtractor.ts   # AI 장소 추출
│   └── types/                  # TypeScript 타입
└── .env.local                  # 환경 변수
```

## 실행 흐름

```
1. 사용자가 웹에서 여행지 입력 (예: "교토")
   ↓
2. YouTube 검색 (타이틀 필터링 + 조회수 상위 20개)
   ↓
3. 자막 수집 (youtube-transcript)
   ↓
4. AI 장소 추출 (OpenAI GPT-4o-mini)
   - 구체적인 상호명만 추출
   - 호텔/일반 지역명/외부 지역 제외
   ↓
5. 중복 제거 및 언급 횟수 계산
   ↓
6. 웹 화면에 결과 표시
   - 카테고리별 분류
   - 구글 지도 링크 제공
```

## AI 프롬프트 전략

### 시스템 메시지
```
당신은 여행 영상 자막에서 구체적인 장소를 추출하는 전문가입니다.
실제 방문 가능한 장소만 추출하고, JSON 형식으로 응답합니다.
```

### 사용자 프롬프트 (핵심 규칙)
```
다음 규칙을 반드시 지켜주세요:
1. ✅ 구체적인 상호명이 있는 장소만 추출
2. ❌ 일반적인 지역명은 제외
3. ❌ "{destination}" 외부 지역의 장소는 제외
4. ❌ 호텔/숙박 시설은 제외
5. ❌ 애매한 표현 제외
6. ✅ 실제 방문 가능한 구체적인 상호가 있는 장소만 포함
7. ✅ "{destination}" 시내 또는 근교(30분 이내)의 장소만 포함
```

## 카테고리 분류

- **맛집**: 레스토랑, 라멘집, 식당
- **카페**: 커피숍, 디저트 카페
- **관광지**: 유명 관광 명소, 전망대, 공원
- **사찰**: 신사, 절, 종교 시설
- **시장**: 전통 시장, 재래시장
- **쇼핑**: 쇼핑몰, 백화점, 상점가
- **액티비티**: 체험 시설, 오락실
- **기타**: 역, 공원, 기타 장소

## 비용 예상

### YouTube API
- 무료: 10,000 units/일
- 영상 20개 검색: ~150 units

### OpenAI API (GPT-4o-mini)
- 입력: $0.150 / 1M tokens
- 출력: $0.600 / 1M tokens
- 영상 20개 처리: ~$0.03-0.08

### Vercel
- 무료 플랜 사용 가능

## 주요 개선 사항

1. **스마트 필터링**: 타이틀에 검색어 포함 + 조회수 상위만
2. **정확한 장소 추출**: AI 프롬프트에 엄격한 규칙 적용
3. **구글 지도 연동**: 각 장소마다 원클릭 지도 검색
4. **반응형 UI**: Tailwind CSS 기반 모던 디자인
5. **실시간 피드백**: 진행 상황 실시간 표시

## 사용 예시

```bash
# 로컬 실행
cd web
npm install
npm run dev

# 브라우저에서 http://localhost:3000 접속
# "교토" 입력 → 검색 시작 → 결과 확인
# 각 장소의 "📍 지도" 버튼 클릭 → 구글 지도에서 위치 확인
```

## 라이선스

MIT
